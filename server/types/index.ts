export type UserInfo = {
  id: string
  name: string
  icon: string
}

export type AuthHeader = {
  authorization: string
}

export type youtubeInfo = {
  videoId: string
  songTitle: string
  songArtist: string
  currentTime: number
}
